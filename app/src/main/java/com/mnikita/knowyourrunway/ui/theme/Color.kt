package com.mnikita.knowyourrunway.ui.theme

import androidx.compose.ui.graphics.Color

// ✅ New coffee palette (light theme)

// Kept old names to avoid breaking imports across screens
val BlackBg = Color(0xFFFFFFFF)        // was dark; now WHITE background
val AccentOrange = Color(0xFF6F4E37)   // was orange; now COFFEE primary

// Extra palette you can use anywhere (optional)
val CoffeePrimary = AccentOrange
val CoffeeDark = Color(0xFF3B2A20)
val CoffeeLight = Color(0xFFB08968)

val Cream = Color(0xFFF7F2EE)
val SurfaceWhite = Color(0xFFFFFFFF)
val SurfaceAlt = Color(0xFFF4ECE6)

val TextMain = Color(0xFF1F1F1F)
val TextSub = Color(0xFF5A5A5A)

val OutlineSoft = Color(0xFFE7DED8)